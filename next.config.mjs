/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['live.staticflickr.com', 'askleo.com', 'www.google.com', 'cdn.bdstall.com', 'i.ebayimg.com', 'cdn.britannica.com', 'encrypted-tbn0.gstatic.com'],
        // unoptimized: true
    },
};

export default nextConfig;
